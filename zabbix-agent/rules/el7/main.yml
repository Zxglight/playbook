---
- name: 安装zabbix rpm包
  yum:
    name: "{{el7_url}}"
    state: present
- name: 安装zabbix agent
  template:
    src: "{{config_file}}"
    dest: "{{dest_config}}"
- name: 启动zabbix agent
  systemd:
    name: zabbix-agent
    state: started
    enabled: yes